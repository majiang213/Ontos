# 结论留痕住工作记录；本体配置只收对象、链、动作、出口

对齐时对候选对的判定，和本体里类与类的关系，是两套清单。专业词：类等价、部分重叠、生命周期、同形异义、子类型。旧称同一、阶段、仅名称相似只作对照。跳过是这次不定的动作，不是一种类与类关系。

**2026-09-01 修订（E1，见纠错清单）**：判定结论与证据是裁决过程的工作记录，住 `adj_decision`（含证据快照），不进本体配置；原决定「还活着的结论写在根列表 class_conclusions」废止。历史草稿里已有的 `class_conclusions` 行（含 `shared` 引用）仍兼容渲染，不迁移。部分重叠不是独立的落地情形：同一概念的表并成一个多源类（来源覆盖面差异是数据事实）；match 链（领域谓词）属于不同概念之间的关联，进 `link_types`。同形异义一行一次，不传递，不合成一团。类等价写成合并挂多源，生命周期写成派生阶段加转化，都不进留痕表以外的任何结论清单。部分与整体、不相交本期不定案。画布的徽章、虚线、芯片改从工作记录投影；历史草稿的旧行仍按旧结论渲染。裁决留痕仍用机器键 `same` / `overlap` / `stage` / `name_similar` / `skip`。

类型级结论不进 `link_types`（那是个体边）。画布是配置的视图：部分重叠的由来虚线、同形异义芯片，都只投影 `class_conclusions`。裁决留痕仍用机器键 `same` / `overlap` / `stage` / `name_similar` / `skip`。
