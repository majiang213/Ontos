# AI Hackathon 2026 提交信息（开放赛道）

> 截止：8月31日 16:00，评审期信息锁定。要素（名称/封面/链接/简介/详情）全非空才参与评审。
> 封面图：`docs/cover-canvas.png`（demo 画布全景：对象卡 + 关系连线 + 工具条）。
> ⚠️ 作品链接需补可访问 URL（见下）。

## 作品名称

**Ontos（安托斯）——现有数据库之上的 AI 读写中间层**

## 作品链接

> ⚠️ 待补：demo 目前只在本地跑（localhost:5199），未部署。选项：
> 1. **最快**：demo 是纯前端静态应用，`vite build` 产物（`build/`）托管到任意静态页面；
> 2. no-code 平台发布（demo 仓库是 no-code 项目形态）；
> 3. 来不及部署用「使用说明」链接：走查文档 `docs/真模型端到端测试.md` 或演示录屏。
> 链接务必提前开好权限。

## 作品简介

现有数据库之上的 AI 读写中间层。Ontos 是通用本体平台：从企业存量数据库里逆向出机器可读的业务本体，架在全部现有数据库之上——查询实时下推各源库拼装作答；写入先核对本体定义、再按映射投影回原表；最难判错的难题——「两个来源的记录是不是同一个东西」，用数据交集率当硬证据、由人定案。完整闭环已在真实代码中通车：MySQL/PostgreSQL/SQLite 三源接入、十个 MCP 工具、462 项测试全绿；服务无状态可水平扩展、并发编辑乐观锁保护。

## 作品详情（全体可见）

### 背景与问题：为什么这一层还没有人做出来

一家真实的公司里，同一个客户的数据散落在采购、设备、财务几套互不相认的现有数据库。想让 AI Agent 替人来办这些事，卡在三道坎上：

| 卡点 | 后果 |
|---|---|
| 数据搬家太贵 | 数仓 / 主数据管理先把数据复制一份再治理——周期以季度计，源头一改全链路跟着改 |
| 模型直连没人敢放权 | 裸 Text-to-SQL 一条幻觉 SQL 就能拖垮生产库；让模型写库等于交出真相的编辑权 |
| 跨系统认人全靠猜 | 同一台设备在三套系统里三个名字，模型分不清谁是谁——答错不是缺陷，是事故 |

适合的客户画像：手里攥着几个历史遗留系统、想让 AI 真正接管一部分业务、但绝不允许有人动这些系统的团队。

### 方案：把本体盖上去，而不是把数据搬出来

数据和系统都不动，把这个领域的共同理解写成一份明确的模型（本体），盖在现有数据库上面当读写中枢。建好它只要三步：

1. **生成对象**：勾选源库的表结构，模型读列名与注释，建议这里藏着哪些业务对象和关系，直接放上画布随手可改——建议只进草稿，已发布的本体一动不动；
2. **裁决**：疑似重复的对象进入待确认面板（不同库，或同一库的两张表）：先确认唯一键（凭什么认定两条记录指向同一个体），再算交集率（两边标识字段取值的重合比例），据此把这两类裁决为类等价 / 部分重叠 / 生命周期 / 同形异义（也可跳过这次）；
3. **发布**：对象、关系、动作定义与全部裁决合并成单一事实源（YAML），带版本号入库，随时回滚到任一历史版本。

发布后，画布回到工作副本（未发布草稿）状态，下一次改动照旧累积为「待发布」；**问数与动作永远只读已发布快照——改到一半的本体，下游永远看不见**。两个入口一套动词：人在画布上做的事与外部 Agent 经 MCP 做的事走同一条校验通道，`propose_objects` 只建议、`edit_draft` 才写工作副本，界面拖拽和远程调用没有特权差异。

**一次完整链路**：接好采购、设备、资产三个库后——第一组疑似重复出现（采购库「在途设备」× 设备库「在役设备」），算序列号交集率约三分之一相同，裁决为**生命周期**（同一批设备的先后时期），两类合为一个对象挂双源，阶段变成读时现算的派生字段，转化动作随之自动建立；第二组设备 × 资产序列号全部重合，裁决为**类等价**，资产台账并入同一对象。发布后问「列出在役设备及其所属部门」——答案是三个源库的实时拼装，附取数路径（这次查询怎么执行的逐步说明）。再执行动作「验收入库」点名一台在途设备：设备源与资产台账各落一行新记录并立起保修卡，再问一次，这台设备的阶段已经是「在役」。

### AI 用在哪里：把不可靠的部分圈起来

对模型的态度是全场最重要的设计决策——**模型只当顾问，不当计算器**：

- **模型只有四个出口**：建模草稿建议、唯一键建议、重复关系倾向、自然语言编结构化请求。查询的执行不含模型——编译、下推、跨源对齐、拼装全是确定性代码，同一个问题永远走同一条取数路径；
- **三层证据链兜底判断**：模型建议是软证据，交集率是硬证据，人定案才是结论。合并判错比不合并更糟，所以涉及合并的关系类型永远由人定案；每次裁决连同当时的证据快照入库留痕；
- **验收问题集当质量闸门**：一批带期望结果的问题挂在每个版本上，「全量跑一遍」失败按阶段落因（编译失败 / 执行出错 / 答案不符）；本体改坏了绿点变红一眼可见；
- **引擎拒绝自由发挥**：配置里没有的动作、属性、关系名一律报错不猜。Agent 拿到的是十个边界清晰的工具，自转的循环留在 Claude Code 这类外部调用方那边——Ontos 内部保持确定性。

一句话：模型负责理解语言和表结构的语义，引擎负责精确执行，人负责只有人该负责的定案。

### 生产级工程能力：按企业形态建造

演示功能的底下是一套按生产标准写的系统：

| 工程能力 | 实现方式（都可验证） |
|---|---|
| 无状态水平扩展 | 进程内没有写队列与内存缓存，草稿与快照全部读平台元库；同一份元库下任意多实例行为一致；元库经一行配置在 SQLite 单文件 / MySQL / PostgreSQL 之间切换，三种方言 DDL 各自独立成文件 |
| 并发编辑互不覆盖 | 每次草稿写入携带修订号做乐观锁更新；检出冲突时画布端自动重读重试叠加应用，接口端返回明确的「草稿已变」 |
| 多端实时协同 | 画布每 2 秒轮询本体的修订号（ETag/304），同事或外部 Agent 的改动两秒内自动刷新画布并弹提示 |
| 领域依赖由测试守门 | 分层方向 schema → infra → features → 路由由结构守门测试强制：查询引擎 import 不到写解释器，读路径依赖不了写路径 |
| 分布式安全发号 | 全局唯一业务编号用雪花号（41 位毫秒 + 10 位实例位 + 12 位序列），多实例各持实例位互不碰撞；创建动作靠源表唯一键加插入后重查兜底实现幂等，动作重发不落双行 |
| 测试永远离线 | 测试套件启动即剥掉模型 Key 与写令牌，不连外网、随时可跑 |

### 治理与安全承诺

平台的每一条承诺都写在架构里，不是口号：

- 业务行永不进平台：平台元库里没有任何业务数据列；
- 问数实时只读查源库，日志记请求与成败、不存结果集；交集比对在内存完成，标识字段的取值集合算完即弃；
- 写入只能执行已发布动作的定义，配置之外的写法引擎一律拒绝；
- 表结构采样脱敏后才进界面；最小权限姿态：问数用只读账号，动作另备可写账号，写端点可再加一道 Bearer 访问令牌；
- 全程审计：每次问数与动作留痕（时间、版本、入参摘要、成败与耗时），每条裁决附证据快照，任何历史版本一键回滚。

### 技术栈

Next.js 16 + React 19 前后端一体 · React Flow 画布 · Vercel AI SDK + Zod 结构化校验（OpenAI 兼容协议，无 Key 时 test 演示空间走剧本实现）· mysql2 / pg / node:sqlite 源库驱动 · 平台元库 SQLite 起步、DSN 切 MySQL / PostgreSQL · 对外 MCP（JSON-RPC 2.0）十个工具 + REST 只读查询。

### 已完成与边界

**已通车**：三种源库连接与脱敏采样 · AI 生成对象草稿 · 候选对发现与交集率计算 · 对齐裁决（类等价 / 部分重叠 / 生命周期 / 同形异义；跳过这次）· 版本化发布与历史回滚 · 自然语言问数（含取数路径）· 动作执行与多源写回 · MCP 十工具与配套 skill · 验收问题集批量回归 · 多工作空间隔离 · 无状态多实例部署。

**当前阶段的边界，白纸黑字讲清**：变更事件已随动作结果返回（`delivered` 字段就位），webhook 外发通道未通；对外鉴权与连接凭据采用最简形态（单一访问令牌 + 凭据明文存于平台元库），排在下一站清单上；子类型裁决留作下一版，没有推理机，表达力止于派生字段的取值规则；跨源对齐在内存中完成，受实例内存上限约束；不做跨库分布式事务——写回部分失败的补偿手段是幂等重发同一动作或人工修库——诚实的小步，好过黑盒的大事务。

### 下一站

告知外发（变更事件送达下游，通知闭环）· 凭据加密与分级授权 · 子类型裁决落地 · 建模质量回归（北星指标 = 人工修改率 + 裁决接受率）· 裁决自动化分级（接受率长期达标的「同形异义」类升级为机器预分拣加人工抽检；合并类无论接受率多高都不毕业——一次错并的代价与一百次对的好处不对称）。第二阶段全部工作的标准只有一个：**让企业敢于把写权限交给 AI——因为每一次写下都有定义、有证据、有回滚。**

## 作品详情（仅评委可见）

- 主库：`ssh://git@git.sankuai.com/~wb_liuxuhao02/ontos.git`——含《Ontology平台MVP设计文档》、ADR 0011/0012（布局与演示剧本决策记录）、真模型端到端走查文档、462 项测试（结构守门 / 纯度边界 / 确定性全覆盖）；
- 演示仓库：`ssh://git@git.sankuai.com/nocode/nocode-b7a7374f691349c4.git`——纯前端静态演示，浏览器内跑完整引擎（内存元库 + 内存源库），`yarn dev` 即开；
- 演示建议路径：test 空间开箱即用（7 个 fixture 连接、15 个本体对象、6 对候选裁决待确认、四波问题集）；空白空间走查（8 套接入系统、10 对裁决剧本：设备一生 + 办公线）见 `docs/真模型端到端测试.md`。
