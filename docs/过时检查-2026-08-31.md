# 两仓库过时检查（2026-08-31）

检查范围：主库 `/Users/majiang/Documents/freespace/js/ontos` 与演示仓库 `/Users/majiang/Documents/freespace/js/nocode-b7a7374f691349c4`。全部结论基于第一手证据（代码行号、命令输出、文件存在性），非文档互引。

## 结论摘要

- **确认过时 ❌：8 条**。核心是「4 个演示连接」这一数字在四处文档/注释里残留（代码实为 7 个）；比赛提交文档内部自相矛盾（test 空间 6 对 vs 10 对）；README 仍指向已被 ADR 0012 替换的旧走查剧本；演示仓库的端到端走查文档整份停留在旧三波世界；`.ontos-demo/` 目录残留已撤系统的库文件。
- **存疑 ⚠️：6 条**。多为「自声明待办」或「无法独立验证」：README 图片全部标注待补素材、demo 仓库无 vitest 依赖无法本地跑测试、「三波 vs 四波」口径并存、demo 仓库文档镜像落后。
- **一致 ✅：19 组**。462 项测试实测全绿、MCP 十工具、15 个本体对象、6 对剧本、四波问题集、12 套演示库、端口/Node 版本、AGENTS.md 全部 43 项登记处、入口对照表全部路由与引擎函数、17 个 op、禁用词零残留、环境变量完全对齐、ADR 0011/0012 全部落地、demo 的 src/server 与主库逐文件一致。

---

## 一、比赛提交文档的数字 vs 代码（docs/比赛提交-2026.md）

| 文档声称 | 实际情况 | 判定 |
|---|---|---|
| 462 项测试 | `npx vitest run` 实测：44 个测试文件、462 项全部通过、全绿（2.75s，离线） | ✅ |
| MCP 十工具 | `src/app/api/[workspace]/mcp/tools.ts` 的 `TOOLS` 数组 10 项：query / run_action / propose_objects / propose_action / list_classes / read_class / search / list_candidates / list_tables / edit_draft | ✅ |
| test 空间 4 个演示连接（:105、:155） | fixture 实为 **7 个**连接：purchase_sys / device_sys / asset_sys / inspect_sys / it_sys / access_sys / oa_sys（`src/server/infra/fixture.ts:51-88` 七个 `d.register`）；`src/tests/mcp.test.ts:212` 断言 `sources.length).toBe(7)`；主库 `docs/真模型端到端测试.md:11` 也写「七个内存 fixture」；demo 浏览器端 `src/ontos/demo/data.ts` 注册 8 个连接 | ❌ |
| test 空间 15 个本体对象 | `src/server/config/ontology.yaml` 15 个 object_types（equipment…requisition） | ✅ |
| test 空间 6 对疑似重复待裁决（:105） | `src/server/infra/llm/demo.ts:249-255` 的 `DEMO_PAIRS` 恰 6 对 | ✅ |
| test 空间「10 对候选待裁决」（:155） | test 空间剧本只有 6 对；10 对是**空白空间走查**剧本（ADR 0012），同文档 :105 自己写「6 对」——文档内部自相矛盾 | ❌ |
| 四波验收问题集 | `src/server/features/acceptance/questionPacks.ts` 的 `QUESTION_PACKS` 4 包（wave1/wave2/wave3/post_accept） | ✅ |
| 十二套演示库 | `src/server/infra/demoSystems.ts:20-32` 的 `DEMO_SYSTEMS` 12 项；`npm run demo:seed` 实测输出 12 行、写出 12 个 .db + 12 个 sidecar | ✅ |
| 端口 6688 | `package.json:7,9` `next dev -p 6688` / `next start -p 6688` | ✅ |
| Node ≥ 22.5 | `package.json:5` `"engines": { "node": ">=22.5" }` | ✅ |
| 封面图 docs/cover-canvas.png（:4） | 文件存在（632KB，提交 77ff0ac 加入） | ✅ |

## 二、文档引用的路径存在性

| 引用 | 存在性 | 判定 |
|---|---|---|
| README:12,60,66,72,76 的 docs/images/hero-canvas.png、demo-import.gif、demo-adjudicate.gif、demo-query.gif、demo-writeback.png | **全部不存在**（docs/images/ 目录整个不存在）——但 README 每处均自标「待补素材」，且附「拍摄清单（提交前删掉本节）」，属已声明的待办而非静默断链；若评审时未补则成断链 | ⚠️ |
| README:180-186 引用的 4 份文档（MVP 设计文档 / ontos-article / 真模型端到端测试 / AGENTS.md） | 全部存在 | ✅ |
| 比赛提交-2026.md:168 引用的 docs/真模型端到端测试.md | 存在 | ✅ |
| docs/cover.png | 存在但**无任何文档引用**（孤儿文件；现行封面是 cover-canvas.png） | ⚠️ |
| 比赛提交-2026.md:159 的 skills/ 四目录、:166 的 npm 命令 | 四目录 SKILL.md 全在；demo:seed / dev / test 脚本全在 | ✅ |

## 三、AGENTS.md 登记处 vs 代码

43 项登记处逐一 grep（主库 src/server + src/components）：**全部存在且位置与登记相符**，包括：

- `NAME_RE` → `src/server/schema/ops.ts:8` ✅；`WORKSPACE_NAME_RE` → `src/server/infra/workspace.ts:15`（const 未导出，位置相符）✅
- `overlapLinksOf` / `homonymPeerMap` / `verdictBadgesOf` → `src/components/canvas/sharedOrigin.ts`（AGENTS.md 只说「画布投影」，位置相符）✅
- `toResult` → `src/server/errors.ts:53`；`logReject` → `:45`；`MSG` → `:71` ✅
- `renderPlaceholders` → `src/server/infra/driver.ts:60`（未导出，AGENTS.md 只登记位置，相符）✅

登记处**文字**的两处口径问题（低危）：

- AGENTS.md:222「三波对错板 `QUESTION_PACKS`」、`questionPacks.ts:1` 文件头注释「三波走查的对错板」——代码实为 **4 包**（wave1/2/3 + post_accept）。比赛提交文档用「四波」，ADR 0012 用「三波 + 验收后」。两种口径并存，建议统一。⚠️

## 四、AGENTS.md 入口对照表 vs 路由/引擎

- 13 个路由全部存在（`src/app/api/[workspace]/` 下）：propose_objects / edit_draft / list_tables / list_sqlite_files / list_candidates / propose_key / compute_overlap / propose_pair / decide / publish / questions / query / mcp，另有 connections / ontology / versions / workspaces ✅
- 引擎函数全部存在：proposeObjects（Llm 接口 `infra/llm/llm.ts:46` + 组合原语 `proposeObjectsFor:63`）、importObjects（`ops/importObjects.ts:11`）、resolveTableInfos（`infra/tables.ts:9`）、listSqliteFiles（`infra/connections.ts:65`）、listCandidates（`integrate/candidates.ts:107`）、proposeKeyFor（`integrate/proposeKey.ts:161`）、computeOverlap / overlapRate（`integrate/overlap.ts:16,45`）、proposePair（`integrate/advise.ts:21`）、decide（`integrate/decide.ts:30`）、adjudicate / applyVerdict（`integrate/applyVerdict.ts:228,129`）、publish / discard（`ontology/versions.ts:16,58`）、editDraft（`ontology/editDraft.ts:17`）、nlToQuery（Llm 接口）、query（`features/query/query.ts:24`）、runAction（`features/action/action.ts:41`）✅
- 17 个 op 与 `src/server/schema/ops.ts` 判别联合一致：15 条内容 op + `save_layout` / `save_edge_bend` 两条界面状态；`mcpDraftOpSchema`（ops.ts:133）恰抽掉 2 条剩 15 条 ✅
- 试算原语 `trial`（proposeKey.ts:83 私有函数）、`candidateKeyColumns`（:39）✅

## 五、禁用词残留

两仓库全量 grep（含 README/AGENTS/比赛文档/全部源码）：

- 「老库」「老数据库」「底盘」：**0 处**（提交 1e60f24 已清干净）✅
- 「内省」「出码」：仅出现在代码注释（src/server、src/tests）、设计文档（docs/Ontology平台MVP设计文档.md）、ADR 0010（工程语境）、AGENTS.md 禁词表与反面教材处。**UI 层（src/components、src/app 用户可见文案）0 处** ✅

## 六、README vs 实现

| README 声称 | 实际情况 | 判定 |
|---|---|---|
| `npm run demo:seed` 写十二套演示库到 .ontos-demo/ | 实测可跑，12 库 + 12 sidecar；.ontos-demo/ 在 .gitignore:12 | ✅ |
| 环境变量全部可选（.env.example） | `.env.example` 6 个变量与代码 `process.env` 读取逐一对应：OPENAI_API_KEY / OPENAI_MODEL / OPENAI_BASE_URL / ONTOS_TOKEN / ONTOS_META_DSN / ONTOS_SNOWFLAKE_INSTANCE_ID（另有 NODE_ENV 为框架标准） | ✅ |
| MCP 端点 `POST /api/<空间名>/mcp`（JSON-RPC 2.0）+ `POST /api/<空间名>/query` | 路由存在；mcp/route.ts:1 注释与 `rpcOk` 信封实现 JSON-RPC 2.0 | ✅ |
| 十个工具清单 | 与 tools.ts 的 10 项逐一对应 | ✅ |
| 画布每 2 秒轮询（ETag/304） | `src/components/revWatcher.ts:93` `setInterval(tick, 2000)` | ✅ |
| 变更事件随结果返回、webhook 未通 | `src/server/features/action/notify.ts:26,62` `delivered: false` | ✅ |
| 测试永远离线 | 实测 2.75s 跑完 462 项，无网络 | ✅ |
| test 空间「四个 fixture 连接」（:136） | 实为 7 个（见第一节） | ❌ |
| **「完整的三波剧本（物 → 人 → 客与货）」照 docs/真模型端到端测试.md 走（:136）** | 该剧本已被 ADR 0012 替换：主库走查文档现为「设备一生 + 办公线 · 八套接入系统」（10 对裁决、四波问题集），标题、接入表、数字全部不同——README 仍指旧剧本 | ❌ |
| 作品链接 https://tabula-rasa-initiate.mynocode.host | curl 实测 HTTP 200 | ✅ |

## 七、ADR 与实现

- **ADR 0011（边感知布局）全部落地**：`src/components/canvas/layout.ts`（按线排、走廊迭代）、`router.ts:401` edgePath 当验收器、`geometry.ts:106` labelPushOf、`schema/ops.ts:104-107` clear_bends/clear_pins 旗标、`src/tests/canvasLayout.test.ts` 有「0 穿节点」「取直率 100%」「宽层不折行」「密集图取直率 ≥ 80%」「长链」等测试 ✅
- **ADR 0012（test 空间剧本）全部落地**：8 系统接入（真模型走查文档表格）、hr_sys/recruit_sys 已撤（demoSystems.ts 无此两项、fixture.ts 注释）、10 对走查候选（文档表格）、四波问题集、黄金数字（engine.test.ts:45 断言 97、:51 断言 81）、画布 19 个对象（8 系统 19 张表）✅
- ADR 0001（sharedObjectName）、0005（候选对成串）、0006（class_conclusions）、0007（动作表单诚实子集）、0008（改标识级联：`ops/editStages.ts` 整文件）、0009（Llm/DemoLlm/AiSdkLlm/getLlm）、0010（proposeKey + trial + candidateKeyColumns + IDENTITY_COL_RULE）结论均有对应代码 ✅

## 八、demo 与主库同步

- `diff -rq src/server`：**完全一致**，唯一差异是 `src/server/config/ontos-meta.db`（二进制运行文件，必然不同）✅
- `diff -rq src/tests`：仅 `m2.test.ts` 有差异——主库多 2 行断言（「剧本外正向启发式仍工作：it_device × it_ticket → Stage」，对应主库提交 208ba13），demo 缺。**demo 是过时方** ❌
- skills/ 目录两仓库完全一致 ✅
- 主库最近提交 77ff0ac 只动 docs（比赛提交 md + 两张封面图），与任务描述一致；`git status` 显示 docs/比赛提交-2026.md 还有一批未提交的「问数→查询」用词微调 ⚠️
- **demo 仓库 docs/真模型端到端测试.md 整份过时**：标题「三波接入 · 十二套源系统」、接入表含已撤的 hr_sys/recruit_sys（candidate/person/appointment 表）、「四个内存 fixture」、「在役 100」、asset「0 行是正常的」、无处置档案/履历/门禁等内容——与 demo 自己镜像的代码（ADR 0012 世界）直接矛盾 ❌
- demo 仓库 AGENTS.md 缺两条登记更新（demoQueries/DEMO_PAIRS 登记、「clear_bends/clear_pins 旗标」表述，仍写「没有独立 op」）；demo docs/adr 只到 0010（缺 0011/0012）——镜像文档落后，代码已同步 ⚠️

## 九、代码注释过时表述

- `src/server/infra/workspace.ts:12` 注释「演示模板与**四个** fixture 连接只属于它」——实为 7 个，注释过时 ❌
- `src/server/features/acceptance/questionPacks.ts:1` 注释「三波走查的对错板」——实为 4 包，口径过时 ⚠️
- 两仓库 src/ 下 **0 处** TODO / FIXME / V2 / 二期 / 后续版本 / 下一版 残留（「暂」仅「暂时没有/暂停」等中性用法）✅

## 十、环境变量与配置

- `.env.example` 与代码读取完全一致（6 个变量，见第六节）✅
- `ONTOS_TOKEN` 写闸门实现于 `src/app/api/_shared.ts:78-81`，与 README 描述一致 ✅

## 十一、测试数量核验

- 主库：`npx vitest run` → **44 文件 / 462 项全部通过**，与文档「462 项」一致 ✅
- demo 仓库：44 个测试文件与主库同源，但 `package.json` 无 vitest 依赖、node_modules 未安装（目录不存在），本地无法独立跑测试；只能由主库镜像结果推断 ⚠️

## 十二、实测过程中发现的环境残留

- `.ontos-demo/` 目录实测存在 **hr.db / recruit.db**（2026-08-26 21:59 时间戳，旧世界播种残留）+ 各自 sidecar，共 4 个文件。`writeDemoFiles`（demoSystems.ts:388）只整份覆盖受管的 12 套，**不清理已从 DEMO_SYSTEMS 撤除的系统文件**；`listSqliteFiles`（connections.ts:65）按目录扫全部 *.db，连接表单下拉会把这些已撤系统列出来（连接名 hr / recruit），与「十二套演示库」宣传不符 ❌

---

### 修复优先级建议

1. 比赛提交文档 :105/:155「4 个演示连接」→ 7 个；:155「10 对候选待裁决」→ 6 对（或改述为「走查剧本 10 对」）。
2. README:136 旧三波剧本描述 → 改指 ADR 0012「设备一生 + 办公线」；:136「四个 fixture 连接」→ 七个。
3. AGENTS.md:93「四个演示 fixture 连接」、workspace.ts:12 注释同步改 7。
4. demo 仓库 docs/真模型端到端测试.md 整份同步主库版本；m2.test.ts 补 2 行断言；AGENTS.md 补登记。
5. writeDemoFiles 增加清理：删除目录内不在 DEMO_SYSTEMS 的 *.db 与 sidecar（或至少告警）。
