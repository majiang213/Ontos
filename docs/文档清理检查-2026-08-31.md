# 文档清理检查（2026-08-31）

检查范围：主库 `/Users/majiang/Documents/freespace/js/ontos` 与演示仓库 `/Users/majiang/Documents/freespace/js/nocode-b7a7374f691349c4` 的 docs/（含 docs/adr/、docs/competition-2026/），外加两仓库根目录 README.md / AGENTS.md / CONTEXT.md 的引用完整性。全部结论基于第一手证据（grep 输出、文件存在性、代码对照），非文档互引。

## 结论摘要

- **🔴 可删：1 个** —— `docs/TODO.md`（孤儿 + 纯历史残留，功能已移除、方向无任何现行文档承接）。
- **🟡 可删但建议留：4 个** —— `docs/提示词审计.md`、`docs/competition-2026/比赛提交-2026.md`、`docs/competition-2026/过时检查-2026-08-31.md`、`docs/competition-2026/cover.png`（均为孤儿但有留存价值；后三者是比赛提交/审计存档，cover.png 已知用户可能用于上传）。
- **🟢 保留：14 个** —— `docs/真模型端到端测试.md`、`docs/Ontology平台MVP设计文档.md`、`docs/adr/0001–0012` 十二份、`docs/competition-2026/cover-canvas.png`（均有引用且内容与代码一致；MVP 文档与 ADR 0003 有两处低危小项，见 ⚠️ 节说明）。
- **⚠️ 内容过时需更新：3 个文件** —— `docs/ontos-article.md`（附录 C 停留在 hr_sys 时代，与真实已发布配置 `src/server/config/ontology.yaml` 脱节，三方矛盾，见下文证据）；demo 仓库 `README.md`（四个 fixture / 三波剧本 / docs/images 断链，与其自身 docs 和代码矛盾）；demo 仓库 `CONTEXT.md`（比主库少「画布·取直率」一节）。另：MVP 设计文档 §7 四周计划未标已完成、§3 M3 一处把未来自动化写成现状（低危，随文档下次修订处理即可）。
- **两库镜像**：主库与 demo 的 17 份 docs 文件（5 根 + 12 ADR）MD5 逐一相同，无 demo 特有过时文档；demo 的 `m2.test.ts`、`AGENTS.md` 已与主库同步（上一轮过时检查指出的问题已修复）。

---

## 🔴 可删（孤儿且无价值 / 被替代）

### 1. `docs/TODO.md` —— 纯历史残留，无任何引用

- **孤儿证据**：全仓库 grep（`grep -rn "docs/TODO.md"`，排除 node_modules/.git/.next）0 处命中；两仓库均如此。README、AGENTS.md、CONTEXT.md、MVP 设计文档、ontos-article.md 均不引用它。
- **内容**：全文 15 行，只讲一件事——「问数 API 台账（移除于 2026-08-21，方向保留）」的移除说明、加回来时该补的清单、旧实现去哪找（`docs/TODO.md:1-15`）。
- **被替代 / 已失效**：该功能在提交 `0474f25`（「移除问数 API 台账」）中删除，TODO.md 即随该提交创建，此后无任何更新。文档自称「方向保留」，但该方向（命名保存的结构化查询）未被任何现行文档承接：README「下一站」清单（`README.md:188-196`）五条均未提；AGENTS.md「问数」词条明文「不留台账」。git 历史本身已完整保留旧实现位置，此文件不提供历史之外的增量信息。
- **判定**：无引用 + 纯历史残留 → 🔴 可删。若「按名重跑查询」的方向仍想留档，建议并入 README「下一站」一句话后删除本文件，而非留一个无人引用的孤儿。

---

## 🟡 可删但建议留（孤儿但有参考价值）

### 2. `docs/提示词审计.md` —— 一次性的提示词判据审计报告

- **孤儿证据**：全仓库 grep「提示词审计」0 处命中（两仓库）；连 AGENTS.md 的登记表、CONTEXT.md 都没提它。
- **参考价值**：2026-08-29 与 ADR 0008 同批产出（提交 `90fb3a6`「提示词判据化…ADR 0008、提示词审计」）。它定义了「判据 / 形状 / 契约」三档口径并逐条对照消费方代码，发现的 3 条形状病均已修复（文件头「结论摘要」：发现①此前已修、②③于 2026-08-29 按 ADR 0008 修完）。文内行号已自注漂移（`docs/提示词审计.md:3-4`），判据结论仍有效，对将来再做提示词审计有参照意义。
- **判定**：孤儿但有参考价值 → 🟡。不删可接受；若删，信息已被 ADR 0008/0010 与代码注释覆盖，无损失。

### 3. `docs/competition-2026/比赛提交-2026.md` —— 比赛提交文档（文件夹内自洽）

- **孤儿证据**：grep「比赛提交-2026」0 处命中（README 不引用它，只引用同文件夹的封面图）。
- **内容有效**：上一轮过时检查指出的数字错误已修复——`比赛提交-2026.md:105`、`:155` 现为「7 个演示连接、15 个本体对象、6 对疑似重复待裁决、四波验收问题集」（与 `src/server/infra/fixture.ts:54-87` 七个 `d.register`、`ontology.yaml` 15 个对象一致）；`:168` 空白空间走查为「八套接入系统、10 对裁决剧本：设备一生 + 办公线」，与 `docs/真模型端到端测试.md`（ADR 0012）一致。
- **判定**：孤儿但属比赛提交存档（用户维护的提交文件夹）→ 🟡。不删。

### 4. `docs/competition-2026/过时检查-2026-08-31.md` —— 同日（今天）的上一轮审计报告

- **孤儿证据**：grep 0 处命中；文件夹内也无人引用它。
- **参考价值**：今日产出的两仓库核对报告。其结论多数已修复——主库 `src/server/infra/workspace.ts:12` 注释现为「七个 fixture 连接」、`questionPacks.ts:1` 现为「四波走查」、README:144 已改指 ADR 0012 剧本、demo 的 `docs/真模型端到端测试.md` 已整份同步（本检查 MD5 验证一致）、demo `AGENTS.md` 与 `m2.test.ts` 已同步、`.ontos-demo/` 残留清理已入代码（`demoSystems.ts:413` 注释「清理已从 DEMO_SYSTEMS 撤除的旧文件」）。未修复项见 ⚠️ 节（即本报告的 ⚠️ 内容，二者是同一批问题的延续）。
- **判定**：孤儿但属当日审计存档 → 🟡。不删（与比赛提交材料同批保留）。

### 5. `docs/competition-2026/cover.png` —— 已知无引用的封面图

- **孤儿证据**：grep「cover.png」0 处命中；现行封面是 `docs/competition-2026/cover-canvas.png`（`README.md:11`、`比赛提交-2026.md:4` 引用它）。
- **判定**：已知背景——用户可能用于上传 → 🟡，标注即可，不删。

---

## 🟢 保留（有引用且内容有效）

### 6. `docs/真模型端到端测试.md` —— 现行走查剧本，与代码一致

- **引用**：`README.md:144`（「照 docs/真模型端到端测试.md 走」）、`README.md:201`（深入阅读表）、`src/server/infra/demoSystems.ts:1`（源注释「…的配方与注释（docs/真模型端到端测试.md，ADR 0012）」）、`比赛提交-2026.md:168`。
- **内容有效**：ADR 0012 世界（八套接入、10 对候选）。黄金数字与代码钉死一致：在役 97 / 在途 81 / 报废 3（`docs/真模型端到端测试.md:12-14` ↔ `src/tests/engine.test.ts:45,51,56` 断言 97/81/3）；「七个内存 fixture」（`:11` ↔ `fixture.ts:54-87`）。
- **一处连带问题见 ⚠️ 节**：它把 test 空间称为「附录 C 模板」（`:12`、`:38`），而附录 C 实际还是旧世界——这是附录 C 的锅，不是本文档的。

### 7. `docs/Ontology平台MVP设计文档.md` —— 设计文档主体有效

- **引用**：`README.md:200`（深入阅读表）、`AGENTS.md:3`（「产品与技术定义见《Ontology平台MVP设计文档.md》」）。
- **内容有效（抽查 §3/§5/§6/§8/§9/附录 A/B）**：M1–M8 模块表、MCP 十工具、`onto_version` 工作行 + `rev` CAS + `draft_key`、雪花发号、`meta_seq` 已删（`MVP:434`）、8 方言 DDL 三份（`MVP:441` ↔ `src/server/meta/ddl/` 实际存在 sqlite/mysql/pg 三份，各 9 张表）、红线与「写与 Agent 边界」均与代码现状一致。§6 的旧 DDL 大块已自标「仅作历史记录」（`MVP:266-269`「下面这份 DDL 是工作空间 B 方案前的形态…现行表结构以 §6『工作空间』节与 src/server/meta/ddl/ 为准」）——自声明的历史，不算问题。
- **两处低危措辞（不删，随下次修订处理）**：
  - `MVP:442-450`「7. 四周计划与验收」是 W1–W4 的进行时计划，未标注「已执行完毕」；其中 W2 验收提到「另备的 10 表测试库（回归评测纳入标注集）」，该评测至今未建（README 下一站 #4「建模质量回归」仍是规划）。建议表头加一句「计划已执行，现状见 README」。
  - `MVP:79` M3 职责写「同形异义类可由机器定案、人抽检」——按 `MVP:59` 自己的「自动化等级是挣来的」与 README 下一站 #5（`README.md:193`「接受率长期达标的『同形异义』类升级为机器预分拣加人工抽检」），这是未来项不是现状，模块表措辞易误读。
  - 另注：`MVP:15` V2.7 变更记录「元库表清单定为 8 张」与 `MVP:259`「现行 9 张」及三份 DDL 实际 9 张（含 `adj_candidates`）不一致——变更记录是历史快照，低危，可不改。

### 8. `docs/adr/0001–0012` —— 十二份 ADR，含自标已废的一份

- **引用**：`AGENTS.md:63`（「改概念先改 CONTEXT，再对 ADR（docs/adr/）」）、ADR 内部互引（`0003:3,5` 自标「已废（被 0004 取代）」、`0009:9` 注「被 0010 扩展」）；落地情况上一轮已逐条核对（过时检查第七节），本检查抽查 ADR 0012 与 `真模型端到端测试.md`/`engine.test.ts` 一致。
- **替代关系核查**：唯一被推翻的是 **ADR 0003（判定办法尚未按所指设计完整）→ 被 0004（判定按三问合成）取代**，0003 文件头已自标「状态：已废」并互相链接——这是 ADR 惯例的留档方式，不是「忘了删的旧文档」，保留正确。其余 ADR 均为增量扩展关系（0009→0010），无推翻。
- **判定**：全部 🟢。

### 9. `docs/competition-2026/cover-canvas.png` —— 现行封面图

- **引用**：`README.md:11`、`比赛提交-2026.md:4`。🟢。

### 附：主题重复核查（检查清单第 3 条）

- **MVP 设计文档 vs ontos-article.md 分工清晰，无大段重复**：`MVP:90` 明文「配置的完整键定义以《ontos-article.md》附录 B 为准，这里只定骨架」；`MVP:505-513` 附录 B 是「细节去文章哪里读」的映射表；`MVP` §4 的演示切片是附录 C 的刻意子集（`MVP:100`「完整可发布配置见《ontos-article.md》附录 C」）。两文档职责不同（设计决策 vs 配置契约），不构成重复。
- **README vs 文档**：README「深入阅读」表（`README.md:199-203`）引用的 4 个文档全部存在且内容有效；AGENTS.md 引用的 `CONTEXT.md`（根目录）、`docs/adr/`、MVP 设计文档、`skills/` 四目录、`.env.example` 全部存在。`skills/_shared/mcp-access.md`、`query-syntax.md` 非孤儿——四个 SKILL.md 以 `<!-- BEGIN SHARED: … -->` 标记引用（如 `skills/ontos-query/SKILL.md:19`），由 `scripts/build-skills.mjs` 内联。

---

## ⚠️ 内容过时需更新（不删但要改）

### 10. `docs/ontos-article.md` 附录 C（及正文 person/调岗示例）—— 停留在 hr_sys 时代，与真实已发布配置脱节

这是本次检查最重的发现。**附录 B 没问题，附录 C 有问题。**

- **附录 B（1320-1468）与 schema 实现逐一核对一致**：
  - 保留字/值来源：`valueSpec.ts:5` `FROM_KEYS = [identity, action, object, current, request, generated]` ↔ 附录 B 值来源与保留字表（`ontos-article.md:1418-1430`）逐项对应；
  - 过滤运算符 `eq/ne/lt/lte/gt/gte/in/contains` ↔ `config.ts:15` `FILTER_OPS`；
  - 效应四操作 `update/create/delete/link` ↔ `config.ts:99-123` `effectItemSchema`；`inform` 的 `object/to/properties` ↔ `config.ts:125-129`；
  - `match` 与 `transition` 互斥 ↔ `config.ts:157-168`（`.refine` 的 `MSG.matchXorTransition`）；
  - `class_conclusions` 的 `overlap/homonym/subtype`、`shared` ↔ `config.ts:171-190`；
  - `generate` 项形状（字面量 / `{ from: … }` / `{ date, format }` / `{ snowflake: true }` / `{ uuid: v7 }`）↔ `src/server/features/query/expr.ts:166-190` `generateValue` 实现逐项对应；
  - `pk`「仅供台账与展示，引擎不读」↔ `config.ts:80` 注释同口径。
  - **旧形状零残留**：`{ sequence: { start, width } }`、`{ add: { left, right } }`、`today`、`yesterday`、`builtin`、`addDays`、`$root` 全仓 grep 0 处（仅以否定句「没有/不用」出现于 `ontos-article.md:1433`，属正确表述）。MVP:15 变更记录中的「`{ sequence }` 计数器」是历史叙述，非配置形状。
- **附录 C（1469-1972）与真实已发布配置脱节**：
  - `src/server/config/ontology.yaml:1-2` 头注释自称「已发布配置 —— 《ontos-article.md》附录 C 全文。引擎加载的就是这份文本」——但两文已不同：ontology.yaml 是 ADR 0012 世界（15 个对象：equipment…requisition，无 person/appointment/change/change_line，无 outlets）；附录 C 是旧世界（12 个对象，含 `person`（`ontos-article.md:1748-1767`，连接 `hr_sys` 在 `:1761`）、`appointment`（`:1801-1847`）、`change`（`:1850-1868`）、`change_line`（`:1871-1891`）、`transfer_post` 调岗动作（`:1769-1799`）、`outlets.payroll`（`:1968-1971`））。
  - **hr_sys 已撤**：`src/server/infra/demoSystems.ts:9` 注释「人事（hr_sys）与招聘（recruit_sys）已撤（ADR 0012）」，`src/tests/demoSeed.test.ts:97` 断言播种文件不含 hr_sys/recruit_sys。附录 C 引用的 `hr_sys` 连接在现行演示世界里不存在，照它手写发布配置无法复现。
  - **三方矛盾**：`docs/真模型端到端测试.md:12`、`:38` 把「附录 C 模板」当 test 空间世界（ADR 0012，在役 97/在途 81）来引用；AGENTS.md:57 与 `:251` 以附录 C 为「完整已发布文本」的唯一锚（「完整已发布文本以附录 C 为准」）——但 test 空间实际加载的是 ontology.yaml（AGENTS.md:93 演示模板），不是附录 C。读者按 AGENTS.md 去附录 C 找「已发布文本」，找到的是已撤系统的世界。
  - **范围说明**：正文 §6.5 告知例子（`ontos-article.md:1145-1237`，change/change_line/outlets/payroll）的形状本身仍有效（`informItemSchema`/`outletSchema` 在 config.ts:125-129、209-212，`features/action/notify.ts` 已实现拼装），问题在「这是已发布配置」的定位已不成立。
- **改法建议**（供后续修订）：把附录 C 同步成 ontology.yaml 的 ADR 0012 世界（15 对象），或至少在附录 C 开头与 ontology.yaml 头注释里把两者的关系改写成「附录 C 是教学用完整示例，test 空间实际配置以 src/server/config/ontology.yaml 为准」，并同步 AGENTS.md:57/:251 与真模型端到端测试.md:12/:38 的「附录 C 模板」表述。正文调岗示例若保留，需明确标注为「示意世界，非 test 空间」。

### 11. demo 仓库 `README.md` —— 与其自身 docs 和代码矛盾（仓库根文件，非 docs/）

- **断链**：`README.md:11,59,64,69,74` 引用 `docs/images/hero-canvas.png`、`demo-import.gif`、`demo-adjudicate.gif`、`demo-query.gif`、`demo-writeback.png`——`docs/images/` 目录不存在（实测 `ls` 报 No such file or directory）。主库 README 已改用 `docs/competition-2026/cover-canvas.png` 并删掉待补素材清单，demo 未同步。
- **数字/剧本过时**：`README.md:136`「自带现成本体和四个 fixture 连接」实为七个（`fixture.ts:54-87`）；「想要完整的三波剧本（物 → 人 → 客与货）…照 docs/真模型端到端测试.md 走」——demo 自己的 `docs/真模型端到端测试.md` 已是「设备一生 + 办公线 · 八套接入系统」（ADR 0012），三波/十二套叙述只剩 `README.md:185`「三波走查的完整演示剧本」及 `:194-198` 的拍摄清单残留。
- **改法建议**：把 demo README 同步成主库 README 的现行版本（fixture 七连、ADR 0012 剧本、封面图路径）。

### 12. demo 仓库 `CONTEXT.md` —— 比主库少一节

- **证据**：`diff ontos/CONTEXT.md nocode-b7a7374f691349c4/CONTEXT.md` 输出 `127,132d126`——主库多出「## 画布 / 取直率」一节（`CONTEXT.md:127-132`，词条「取直率」：画布直连边占比、重排后 ≥ 80% 算过、0 条线穿节点是硬保证），demo 缺失。CONTEXT.md 是概念定义唯一出处，demo 镜像应补齐。
- **低危**：纯概念条目缺失，不影响运行；demo 的 AGENTS.md 已与主库一致（diff 无差异），说明同步是常态，缺这一节是遗漏。

---

## 附：两仓库镜像一致性核查（检查清单第 8 条）

- 主库与 demo 的 17 份 docs 文件（`提示词审计.md`、`真模型端到端测试.md`、`Ontology平台MVP设计文档.md`、`ontos-article.md`、`TODO.md`、`docs/adr/0001-0012`）**MD5 逐一相同**（`md5 -q` 比对全同），即 demo 的 docs 无独立于主库的过时问题；demo 无 `competition-2026/` 目录（比赛材料只在主库）。
- demo `src/tests/m2.test.ts` 与主库 diff 无差异（上轮指出的缺 2 行断言已补）；demo `AGENTS.md` 与主库 diff 无差异（上轮指出的缺登记已补）。
- 上轮过时检查列出的代码注释问题均已修复：`workspace.ts:12` 现为「七个 fixture 连接」、`questionPacks.ts:1` 现为「四波走查」。

## 汇总

| 档位 | 数量 | 文件 |
|---|---|---|
| 🔴 可删 | 1 | docs/TODO.md |
| 🟡 可删但建议留 | 4 | docs/提示词审计.md、docs/competition-2026/比赛提交-2026.md、docs/competition-2026/过时检查-2026-08-31.md、docs/competition-2026/cover.png |
| 🟢 保留 | 14 | docs/真模型端到端测试.md、docs/Ontology平台MVP设计文档.md、docs/adr/0001–0012、docs/competition-2026/cover-canvas.png |
| ⚠️ 内容过时需更新 | 3 | docs/ontos-article.md（附录 C）、demo README.md、demo CONTEXT.md（另：MVP 文档 §7/§3 两处低危措辞） |

真正建议动手删的只有 1 个（TODO.md）；真正需要改内容的是 3 个（ontos-article.md 附录 C 优先——它被 AGENTS.md 定为「已发布文本」的唯一锚，与代码脱节影响最大）。
