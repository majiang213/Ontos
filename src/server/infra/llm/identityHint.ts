// 唯一键怎么选 —— 判据唯一出处：跨源能对上同一个体的业务编号列。
// 两层分开：最终判定「不要按列名前后缀猜」（形状证明不了唯一，_no/_id 结尾同样可能是自增代理键）；
// 候选召回「按名称/注释带业务编号特征捞进试算」只负责进门，准不准由数据试算与实现判定，两层不冲突。
// 真模型提示词用文案版（aiSdk.ts）；演示实现没有语义可读，只落机械部分——主键本身是业务编号（非整数）
// 或带唯一约束的非整数列才当唯一键（demo.ts）。一处改，两处不漂移。

/** 同一条规则的自然语言版（进提示词）。 */
export const IDENTITY_COL_RULE =
  "identity（唯一键）选跨源能对上同一个体的业务编号列：取值是业务发给个体的编号（序列号、单号、证件号这类），唯一且稳定；" +
  "列名什么形状都可能，按列义与列注释判断，不要按 _no/_id 之类前后缀猜；" +
  "列带 unique 标记（数据库唯一约束）是「唯一」的硬证据：非整数、语义是业务编号的唯一列直接当 identity；" +
  "自增代理主键（整数 pk）只是表内行号，跨源对不上号，即使唯一也不能当 identity；" +
  "多列主键、多列唯一索引的单列不唯一，不能当 identity（pk / unique 标记可能只是复合键的一员）；" +
  "没有合格的列就省略 identity，留给人定";

/** 名称召回：业务编号特征后缀——只召回进试算，不作判定（判定归数据与实现；_id 不进召回：自增代理键最常见的后缀）。 */
export const KEY_HINT_NAME_RE = /(no|code|sn|num)$/i;
/** 注释/说明召回：白话里的编号词。 */
export const KEY_HINT_DESC_RE = /(编号|序列号|单号|证件号|卡号|号码)/;
