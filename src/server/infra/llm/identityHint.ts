// 识别字段（唯一键）怎么选 —— 判据唯一出处：跨源能对上同一个体的业务编号列。
// 形状证明不了唯一（_no/_id 结尾同样可能是自增代理键），不按列名前后缀猜。
// 真模型提示词用文案版（aiSdk.ts）；演示实现没有语义可读，只落机械部分——主键本身是业务编号（非整数）才当识别字段（demo.ts）。一处改，两处不漂移。

/** 同一条规则的自然语言版（进提示词）。 */
export const IDENTITY_COL_RULE =
  "identity（唯一键）选跨源能对上同一个体的业务编号列：取值是业务发给个体的编号（序列号、单号、证件号这类），唯一且稳定；" +
  "列名什么形状都可能，按列义与列注释判断，不要按 _no/_id 之类前后缀猜；" +
  "自增代理主键（整数 pk）只是表内行号，跨源对不上号，不能当 identity；没有合格的列就省略 identity，留给人定";
