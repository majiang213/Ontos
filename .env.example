# 复制为 .env 后按需填写。全部可选：不设也能 npm run dev
# （离线确定性回退 + 本地 SQLite 元库）。Next.js 会自动加载根目录 .env。

# ---------- 真模型（OpenAI 兼容协议）----------
# 不设 KEY = 问数编译 / 逆向建模 / 疑似重复建议三个槽位走离线回退。
# 问数离线剧本只覆盖 test 演示空间；生成对象与候选对建议是通用启发式，各空间都能用。
# 演示数据不按 Key 判断：test 空间有无 Key 都有演示模板与 fixture 连接，其余空间永远空白起步。
# 设了 KEY 则 MODEL 必填（不设会报错）。BASE_URL 不设走 @ai-sdk/xai 默认端点 https://api.x.ai/v1。
# 任何 OpenAI 兼容端点均可（含内部网关）。

# OPENAI_API_KEY=
# OPENAI_MODEL=
# OPENAI_BASE_URL=https://api.x.ai/v1

# ---------- 写端点令牌闸 ----------
# 不设 = 写端点不验令牌（演示默认放开）。
# 设了之后，连接 / 发布 / 裁决 / 动作等写库 API 需 Authorization: Bearer <token>。
# 本地浏览器走查不要设：画布写请求配不上令牌会 401。

# ONTOS_TOKEN=

# ---------- 平台元库 ----------
# 不设 = 单文件 SQLite src/server/config/ontos-meta.db（本地开发 / 单实例）。
# mysql://… 走 MySQL。多实例部署必须用 MySQL（SQLite 单文件不能多实例共享）。

# ONTOS_META_DSN=mysql://user:pass@host:3306/ontos_meta

# ---------- 雪花号实例位 ----------
# 0–1023。多实例部署必配：同一 MySQL 元库下每实例一个不重复的值，保证发号不撞。
# 不设 = 随机派生（碰撞概率极低但存在；单实例无所谓）。

# ONTOS_INSTANCE_ID=
