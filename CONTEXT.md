# Ontos

一份可发布的本体，盖在已有系统之上：规定领域里有什么，两个体何时算同一个，类与类是什么关系。个体留在源库；问数和动作按这份规定访问原库。

## 存在

**本体**：
对一个领域的概念化的明确、可发布说明：类、属性、关系、同一性标准。它约束「有什么」，不是某张表怎么建。
_Avoid_: 新库、对象存储、OWL 推理机

**类**：
对领域里一种事物的分类，如人、设备、订单。
_Avoid_: 表、节点（表是原料；节点是类在画布上的画法）

**属性**：
类的特征，取值是字面值（文本、数、日期、真假）。
_Avoid_: 列（列是源表上的）；界面叫字段

**个体**：
类所描述的具体事物。平台不保存个体，个体只在源库。
_Avoid_: 行、记录（行是源表上的）

**同一性标准**：
判定两条记录是否指向同一个体所凭的属性。须能区分、能覆盖、稳定。源表主键只定位行，可以不是同一性标准。
_Avoid_: 主键、identity 直出；界面叫唯一键

**关系**：
把一类的个体连到另一类的个体上。取值是另一个体，不是字面值。

**源映射**：
一个类的属性与某张源表的列之间的对照。一个类可挂多个源。

**公共属性**：
两类都用来描述重合个体的属性。同名是认出它的一种办法，不是它的定义。
_Avoid_: 同名字段（那是落地时能否上移的条件，不是这个概念本身）

## 类与类

类与类的关系（统称**类关系**）不是一张封闭清单。个体之间的边见「关系」。下面各项各是什么。对齐候选对时只问其中若干项（是不是同一种东西、同一批个体、同一个体的不同时期），不是这张清单的穷尽。

对齐时问的是已经上画布的一对候选类。这一对属于某一串疑似重复。可以来自两个库，也可以是同一库的两张表。不是本体里任意挑两个类。

判定看实质：它们是不是在描述同一种东西、是不是同一批个体、是不是同一批个体的不同时期。名字不同、表是多行还是多列、字段是否同名，都不能当区分标准，也不能当否决。

**类等价**：
两个类描述同一种现实事物。一边源上还没有行，不能据此否定。
_Avoid_: 两个名字指同一类（名字不是判定）；合并（那是落地）；同一（旧称）

**部分重叠**：
两个类描述的个体有交集、又不是同一批（各有对方覆盖不到的个体）。
_Avoid_: 子类型；部分与整体；靠字段同不同名来区分类等价与部分重叠

**生命周期**：
两个类描述的是同一个体的不同时期。阶段不是新个体。
_Avoid_: 阶段（旧称）

**时期标识**：
生命周期两端的 key：早阶段 from、晚阶段 to；进值域取值、派生规则产出、转化关系名与动作字面量（给人看的一半是时期中文名）。建议时原样取自两类现成值域的取值、按时间先后；没有现成值域，引擎给中性占位 early / late（中文名 早期 / 晚期），人事后改标识换领域词。
_Avoid_: 模型自造词；写死某一领域的词（如设备词）

**同形异义**：
两个类不是在描述同一种东西。即使名字像，也不是类等价。
_Avoid_: 仅名称相似（旧称）；不相交

**子类型**：
一类的每个个体同时是另一类的个体，并继承其全部属性。本期不定案。
_Avoid_: 部分重叠；部分与整体；上位对象

**部分与整体**：
组成关系。部分的个体不是整体的个体。本期不定案。
_Avoid_: 子类型；部分重叠

**不相交**：
两类没有共同的个体。本期不定案。
_Avoid_: 同形异义（不是同一种东西，个体之间仍可以有关系）

**上位对象**：
部分重叠成立之后，为公共部分另立的类。不带继承；属性是移上去的。没有可上移的公共属性时，它仍然成立，只带同一性标准。
_Avoid_: 父类、继承；子类型的上位类；界面尽量说公共对象

## 判定与证据

**判定**：
对这一对候选类，断言对齐所问的那一项成立。不是断言类与类关系的全集。问的是它们描述的现实，不是它们叫什么、表长什么样。

**定义** 与 **判定办法** 不是一件事。定义说各项是什么；判定办法说对齐时靠什么认出来。

**判定办法**：
对这一对候选类问三问。第一问同一种事物：人定案；命中大于零是硬线索（对得上号的个体是同一个体），命中为零不能否定。第二问同一批个体：交集率；一侧取不出取值则沉默。第三问不同时期：人定案，状态和日期只是线索。三问合成一项对齐结论。同一种、有交集又不是全交、不是时期：类等价与部分重叠都可能，人选是并成一个类还是保留两个类。命中大于零时数据不支持同形异义；命中为零时数据不支持部分重叠。

**入围**：
已上画布、尚未定案的两个有来源的类，成为一对候选。可以是不同库，也可以是同一库里的两张表。结构相似不是资格。结构比对列出可能有关的对，会漏、会错。没被任何对牵上的类，不进任何一串。

**落地**：
把已经判定的类与类结论写进本体，问数和动作按这份结构执行。落地不是判定的依据。

**建议**：
按类名、连接、字段名给出「可能有关」的对和软倾向。不定「同一种事物」。不看表里的行。结构可以完全不同仍是类等价，所以会漏、会错。

**裁决**：
人对**这一对**按三问合成结论。一次只裁一对。裁完，还没问完的仍在这一串里；上一对的结论不代替下一对的三问。

**跳过**：
对这一对先不定这次对齐要判的那一项。不是类与类的关系。
_Avoid_: 当成一种类与类关系；当成子类型

**疑似重复串**：
通过候选对能牵到一起的一组类。A 与 B 是一对、B 与 C 是一对，则 A、B、C 是一串。
_Avoid_: 簇、连通分量、独立题、一次裁整串；界面不出现这个词，待确认仍一次一对

**交集率**：
两端同一性标准取值（归一化后）的集合重合度。只回答同一批个体。一侧取不出取值则沉默。一行一个体取对照列；一列一个体取列所标识的个体（本期只落地行存）。

**候选对**：
一串里正在被问的两个类（不同库，或同一库的两张表）。被问的是：描述的是不是同一种或同一批现实事物。一对的结论会改这一串里还该问谁，不改下一对该判成哪一种。
_Avoid_: 界面入口叫待确认、区块叫疑似重复；互不相干的两道题

## 边界

**数据边界**：
不迁移、不复制业务个体；问数实时读源库；动作按效应和源映射写回原库。
_Avoid_: 界面不出现「数据边界」四字

**工作副本**：
尚未发布的本体。问数与动作读已发布快照，不读工作副本。
_Avoid_: 草稿当问数真相
